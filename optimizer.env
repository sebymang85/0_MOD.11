# ===========================================
# OPTIMIZER CONFIG - ABILITAZIONE PARAMETRI
# ===========================================
# 1 = il parametro è OTTIMIZZATO (usa MIN/MAX/STEP)
# 0 = il parametro è DISATTIVATO (il valore è preso da simulator.env)

VOLUME_LOOKBACK_ENABLED=1
VOLUME_LOOKBACK_MIN=18
VOLUME_LOOKBACK_MAX=24
VOLUME_LOOKBACK_STEP=6

VOLUME_THRESHOLD_ENABLED=1
VOLUME_THRESHOLD_MIN=1
VOLUME_THRESHOLD_MAX=3
VOLUME_THRESHOLD_STEP=1

VOLUME_THRESHOLD_MAX_ENABLED=0
VOLUME_THRESHOLD_MAX_MIN=1000000000000000000000000000000000000000000000
VOLUME_THRESHOLD_MAX_MAX=1000000000000000000000000000000000000000000000
VOLUME_THRESHOLD_MAX_STEP=6

PRICE_INCREASE_WINDOW_ENABLED=1
PRICE_INCREASE_WINDOW_MIN=12
PRICE_INCREASE_WINDOW_MAX=12
PRICE_INCREASE_WINDOW_STEP=2

MIN_PRICE_INCREASE_PERCENT_ENABLED=1
MIN_PRICE_INCREASE_PERCENT_MIN=4
MIN_PRICE_INCREASE_PERCENT_MAX=4
MIN_PRICE_INCREASE_PERCENT_STEP=2.5

MIN_CURRENT_VOLUME_ENABLED=0
MIN_CURRENT_VOLUME_MIN=1
MIN_CURRENT_VOLUME_MAX=10000
MIN_CURRENT_VOLUME_STEP=1000

MAX_CURRENT_VOLUME_ENABLED=0
MAX_CURRENT_VOLUME_MIN=0
MAX_CURRENT_VOLUME_MAX=0
MAX_CURRENT_VOLUME_STEP=1

SPIKE_COOLDOWN_CANDLES_ENABLED=1
SPIKE_COOLDOWN_CANDLES_MIN=12
SPIKE_COOLDOWN_CANDLES_MAX=120
SPIKE_COOLDOWN_CANDLES_STEP=12




PRICE_RATIO_AT_SPIKE_MODE_ENABLED=0

PRICE_RATIO_AT_SPIKE_MIN_ENABLED=1
PRICE_RATIO_AT_SPIKE_MIN_MIN=-1000000
PRICE_RATIO_AT_SPIKE_MIN_MAX=-1000000
PRICE_RATIO_AT_SPIKE_MIN_STEP=1

PRICE_RATIO_AT_SPIKE_MAX_ENABLED=1
PRICE_RATIO_AT_SPIKE_MAX_MIN=14
PRICE_RATIO_AT_SPIKE_MAX_MAX=30
PRICE_RATIO_AT_SPIKE_MAX_STEP=2

UTC_HOUR_MIN_ENABLED=0
UTC_HOUR_MIN_MIN=0
UTC_HOUR_MIN_MAX=0
UTC_HOUR_MIN_STEP=2

UTC_HOUR_MAX_ENABLED=0
UTC_HOUR_MAX_MIN=23
UTC_HOUR_MAX_MAX=23
UTC_HOUR_MAX_STEP=2

UTC_WEEKDAY_ENABLED=0

HL_MIN_PCT_ENABLED=1
HL_MIN_PCT_MIN=10
HL_MIN_PCT_MAX=20
HL_MIN_PCT_STEP=1

BODY_RATIO_MIN_ENABLED=1
BODY_RATIO_MIN_MIN=0.7
BODY_RATIO_MIN_MAX=0.9
BODY_RATIO_MIN_STEP=0.1

# ===========================================
# OUTPUT
# ===========================================

# Nome del file CSV di output dell'optimizer
OPTIMIZER_OUTPUT_CSV=optimizer_results.csv

OPTIMIZER_WORKERS=24
OPTIMIZER_CHUNK_SIZE=auto
LOG_LEVEL=ERROR

OPTIMIZER_SPIKES_BATCH_SIZE=1000

OPTIMIZER_PROFILE=1
CORE_EXTREMA_MODE=precompute